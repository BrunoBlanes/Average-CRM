@page
@model LoginModel;

@{
	ViewData["Title"] = "Log in";
}

<div id="login-main-div" class="content">
	<section>
		<form id="account" method="post">
			<h4>Use a local account to log in.</h4>
			<div>
				Email:
				<fluent-text-field id="Email" name="Email" type="email" data-val="true" />
				<span asp-validation-for="Email" class="text-danger"></span>
			</div>
			<div>
				Password:
				<fluent-text-field id="Password" name="Password" type="password" data-val="true" />
				<span asp-validation-for="Password" class="text-danger"></span>
			</div>
			<div>
				<fluent-checkbox id="RememberMe" name="RememberMe" type="checkbox" value="true">
					@Html.DisplayNameFor(m => m.RememberMe)
				</fluent-checkbox>
			</div>
			<div>
				<fluent-button appearance="accent" type="submit">Log in</button>
			</div>
			<div>
				<p>
					<a id="forgot-password" asp-page="./ForgotPassword">Forgot your password?</a>
				</p>
				<p>
					<a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Register as a new user</a>
				</p>
				<p>
					<a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Resend email confirmation</a>
				</p>
			</div>
		</form>
	</section>
</div>

<script>
	JsFunctions.StopScroll("login-main-div");
</script>

@section Scripts {
	<partial name="_ValidationScriptsPartial" />
}